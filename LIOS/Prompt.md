---
aliases:
  - Befehl
---
Der Prompt ist die Eingabe eines [[Terminal#Interpreter|Interpreters]] und besteht im Normalfall aus einem **Programm**, mindestens einer **Option** und mindestens einem **Argument**.
```bash
ls -l /home
```
`ls` -> Programm
`-l` -> Option
`/home` -> Argument
